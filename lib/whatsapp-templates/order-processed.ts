export function generateOrderProcessedBuyerWA(orderShortId: string, customerName: string): string {
  return `Halo ${customerName}! 👋

Pesanan #${orderShortId} sedang kami proses dan siapkan untuk dikirim 📦

Kami akan kirim notifikasi saat barang sudah dikirim.

Terima kasih sudah belanja di EVC Mercato 💚`
}
