-- Enhance orders table
ALTER TABLE orders
  ADD COLUMN IF NOT EXISTS shipping_address_id UUID REFERENCES addresses(id) ON DELETE SET NULL,
  ADD COLUMN IF NOT EXISTS shipping_recipient_name VARCHAR(100),
  ADD COLUMN IF NOT EXISTS shipping_phone VARCHAR(20),
  ADD COLUMN IF NOT EXISTS shipping_full_address TEXT,
  ADD COLUMN IF NOT EXISTS shipping_city VARCHAR(100),
  ADD COLUMN IF NOT EXISTS shipping_province VARCHAR(50),
  ADD COLUMN IF NOT EXISTS shipping_postal_code VARCHAR(10),
  ADD COLUMN IF NOT EXISTS shipping_method VARCHAR(20) DEFAULT 'reguler',
  ADD COLUMN IF NOT EXISTS shipping_cost INTEGER NOT NULL DEFAULT 10000,
  ADD COLUMN IF NOT EXISTS shipping_cost_discount INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS service_fee INTEGER NOT NULL DEFAULT 3000,
  ADD COLUMN IF NOT EXISTS service_fee_discount INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS subtotal INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS total_amount INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS xendit_invoice_id VARCHAR(100),
  ADD COLUMN IF NOT EXISTS xendit_invoice_url TEXT,
  ADD COLUMN IF NOT EXISTS xendit_payment_method VARCHAR(50),
  ADD COLUMN IF NOT EXISTS paid_at TIMESTAMPTZ,
  ADD COLUMN IF NOT EXISTS shipped_at TIMESTAMPTZ,
  ADD COLUMN IF NOT EXISTS tracking_number VARCHAR(50),
  ADD COLUMN IF NOT EXISTS cancelled_at TIMESTAMPTZ,
  ADD COLUMN IF NOT EXISTS notes TEXT;
